import React from 'react';
import {connect, css, styled } from "frontity";

const BriefContent = ({state}) => {
      

    return (
<h1>Soy brief</h1>
    );
}
 
export default connect(BriefContent);